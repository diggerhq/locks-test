projects:
  - name: dev
    dir: dev
  - name: prod
    dir: prod

workflows:
  default:
    plan:
      steps:
        - init
        - plan
        - run: echo $PR_BRANCH $DEFAULT_BRANCH $PROJECT_DIR $PROJECT_NAME
